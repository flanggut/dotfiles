set-option -g default-shell /usr/local/bin/fish
set -g prefix2 C-a                        # GNU-Screen compatible prefix
bind C-a send-prefix -2
set -g mode-keys vi
set -q -g status-utf8 on                  # expect UTF-8 (tmux < 2.2)
setw -q -g utf8 on
set -g history-limit 5000                 # boost history
set -sg escape-time 0                     # because VIM

# -- display -------------------------------------------------------------------

set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows
setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed
set -g set-titles on          # set terminal title
set -g display-panes-time 800 # slightly longer pane indicators display time
set -g display-time 1000      # slightly longer status messages display time
set -g status-interval 10     # redraw status line every 10 seconds

# activity
set -g monitor-activity on
set -g visual-activity off

#+ Status +
set -g status-bg colour0
set -g status-fg colour8
#+--- Bars ---+
set -g status-left "#[fg=colour0,bg=colour6,bold] #S #[fg=colour6,bg=colour0,nobold,noitalics,nounderscore]"
set -g status-right "#{prefix_highlight}#[fg=colour8,bg=colour0,nobold,noitalics,nounderscore]#[fg=colour8,bg=colour0] %Y-%m-%d #[fg=colour8,bg=colour0,nobold,noitalics,nounderscore]#[fg=colour8,bg=colour0] %H:%M #[fg=colour6,bg=colour0,nobold,noitalics,nounderscore]#[fg=colour0,bg=colour6,bold] #H "

#+--- Windows ---+
set -g window-status-format "#[fg=colour0,bg=colour0,nobold,noitalics,nounderscore] #[fg=colour6,bg=colour0]#I #[fg=colour6,bg=colour0,nobold,noitalics,nounderscore] #[fg=colour6,bg=colour0]#W #F #[fg=colour0,bg=black,nobold,noitalics,nounderscore]"
set -g window-status-current-format "#[fg=colour0,bg=colour6,nobold,noitalics,nounderscore] #[fg=colour0,bg=colour6]#I #[fg=colour0,bg=colour6,nobold,noitalics,nounderscore] #[fg=colour0,bg=colour6]#W #F #[fg=colour6,bg=black,nobold,noitalics,nounderscore]"
set -g window-status-separator ""

# -- navigation ----------------------------------------------------------------

bind -r p send -t -1 C-p Enter
bind -r C-p send -t -1 C-p Enter

# split current window horizontally
bind - split-window -v -c '#{pane_current_path}'
# split current window vertically
bind _ split-window -h -c '#{pane_current_path}'

# pane navigation
bind -r h select-pane -L  # move left
bind -r j select-pane -D  # move down
bind -r k select-pane -U  # move up
bind -r l select-pane -R  # move right
bind > swap-pane -D       # swap current pane with the next one
bind < swap-pane -U       # swap current pane with the previous one

# maximize current pane
bind + break-pane

# pane resizing
bind -r H resize-pane -L 2
bind -r J resize-pane -D 2
bind -r K resize-pane -U 2
bind -r L resize-pane -R 2

# window navigation
bind -r C-h previous-window # select previous window
bind -r C-l next-window     # select next window
bind Tab last-window        # move to last active window

# -- copy mode -----------------------------------------------------------------
bind Enter copy-mode # enter copy mode
run -b 'tmux bind -t vi-copy v begin-selection 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi v send -X begin-selection 2> /dev/null || true'
run -b 'tmux bind -t vi-copy C-v rectangle-toggle 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi C-v send -X rectangle-toggle 2> /dev/null || true'
run -b 'tmux bind -t vi-copy y copy-selection 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi y send -X copy-selection-and-cancel 2> /dev/null || true'
run -b 'tmux bind -t vi-copy Escape cancel 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi Escape send -X cancel 2> /dev/null || true'
run -b 'tmux bind -t vi-copy H start-of-line 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi H send -X start-of-line 2> /dev/null || true'
run -b 'tmux bind -t vi-copy L end-of-line 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi L send -X end-of-line 2> /dev/null || true'

# reload configuration
bind r source-file ~/.tmux.conf \; display '~/.tmux.conf sourced'
